/**
 * Sincronización con API municipal de vehículos.
 * Se ejecutará automáticamente cuando la integración esté disponible.
 */
export async function syncGovVehicles(_userId: string): Promise<number> {
  return 0;
}
